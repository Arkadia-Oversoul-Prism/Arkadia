# Serve Gate Script

This script serves the repository root on a local HTTP server and opens the Living Gate in your default browser.

Usage:
```bash
chmod +x scripts/serve-gate.sh
./scripts/serve-gate.sh 8000
```

If no port is specified, it defaults to `8000`.
