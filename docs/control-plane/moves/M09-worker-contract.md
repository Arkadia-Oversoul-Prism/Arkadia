# M09 — Worker Contract

Status: PENDING
Depends on: M08

Objective: define the worker interface described by AEAS.

The contract covers wake, load, validation, task loading, bounded sessions, execution, checkpointing, verification, reporting, termination, and resumability.

The worker remains subordinate to the approved trajectory and task scope. It does not redefine the specification or governance model.

Acceptance: Architect -> Engineering Lab -> Worker -> Agent -> Repository -> Evidence -> Human Review is represented as one inspectable contract.

Verification: contract schema checks, lifecycle examples, checkpoint examples, and documentation consistency.

Stop at review.
