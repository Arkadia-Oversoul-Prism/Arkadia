# WEAVER-K1 — Reconnaissance & Context Packet

## Purpose

Deterministic orientation layer: repository identity, architecture references, Weaver capabilities, checkpoints, tests/builds inventory, protected boundaries.

**CONTEXT ≠ AUTHORIZATION.** Next action defaults to `awaiting human authorization`.

## Artifact

`data/weaver/context/current.json` — schema_version 1.0.0

## Module

`weaver/recon.py` — `build_context_packet`, `write_context_packet`, `is_stale`

## Governance

Operates under K0.1 PassSpec / publication rules. Does not expand scope or grant passes.
